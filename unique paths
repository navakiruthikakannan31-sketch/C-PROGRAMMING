#include <stdio.h>

int uniquePaths(int m, int n) {
    long long res = 1;
    for (int i = 1; i < m; i++) {
        res = res * (n + i - 1) / i;
    }
    return (int)res;
}

int main() {
    int m = 3, n = 7;
    printf("%d\n", uniquePaths(m, n));
    return 0;
}
